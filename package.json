{
	"name": "equip-gen",
	"version": "0.0.1",
	"private": true,
	"scripts": {
		"dev": "vite dev",
		"build": "vite build",
		"preview": "vite preview",
		"check": "svelte-kit sync && svelte-check --tsconfig ./jsconfig.json",
		"check:watch": "svelte-kit sync && svelte-check --tsconfig ./jsconfig.json --watch",
		"dep": "gh-pages -d build -t true"
	},
	"devDependencies": {
		"@sveltejs/adapter-static": "^3.0.10",
		"@sveltejs/kit": "^2.57.1",
		"svelte": "^5.55.3",
		"svelte-check": "^4.4.6",
		"typescript": "^6.0.2",
		"vite": "^6.4.2"
	},
	"type": "module",
	"pnpm": {
		"peerDependencyRules": {
			"allowedVersions": {
				"vite": "5"
			}
		}
	},
	"dependencies": {
		"@sveltejs/vite-plugin-svelte": "^3.1.2"
	},
	
	"resolutions": {
		"cookie": "^0.7.0",
		"esbuild": "^0.25"
	}

}